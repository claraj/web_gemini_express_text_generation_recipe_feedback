    <div class="header bg-secondary-subtle mb-4 p-4 rounded-3 text-center">
        <h1>Cheap, healthy recipes with your leftovers</h1>
    </div>
<p>Enter the ingredients you have, and any dietary needs. We'll create a custom recipe with what you have on hand.</p>
    {{!-- <form id="ingredient-form" method="POST" action="/generate_recipe_html"> --}}
    <form id="ingredient-form" method="POST" action="/generate_recipe_json">
        <label class="form-label" for="ingredients">Name</label>
        <textarea class="form-control" id="ingredients" name="ingredients">cheese bread</textarea>
        <input id="submit-button" class="btn btn-primary" type="submit">
    </form>
</div>  

<script>

    let ingredientForm = document.querySelector('#ingredient-form')
    let ingredientSubmitButton = document.querySelector('#submit-button')

    // Ensure button is enabled and says "Submit" when the page is loaded
    window.addEventListener('pageshow', function() {
    ingredientSubmitButton.disabled = false
    ingredientSubmitButton.value = "Submit"
    })

    // Disable button and change button text to "Submitting..." when the user 
    // has entered valid data in the form and clicked the submit button 
    ingredientForm.addEventListener('submit', function() {
        ingredientSubmitButton.value = "Creating..."
        ingredientSubmitButton.disabled = true
    })
    
</script>